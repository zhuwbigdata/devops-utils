README
# Help with -
$ man docker-network-create

# For TLS/SSL certificates, use mounted volumes.

# docker logs CMD can be run when the container is stopped or running.
$ docker logs $docker_container 

# docker exec CMD can only be run wheh the container is running.
$ docker exec $docker_container /bin/bash $bash_command
$ docker exec -u $uid $docker_container /bin/bash  

# docker image CMD 
$ docker rmi $image_id

# docker network 
$ docker network ls
# docker inspect by name 
$ docker network inspect [host|bridge|none]
